# shellcheck shell=bash
#
# Available shell commands.

# shellcheck source=/dev/null
source "${CURRENT_DIR}/commands/dashboard.sh"

# shellcheck source=/dev/null
source "${CURRENT_DIR}/commands/down.sh"

# shellcheck source=/dev/null
source "${CURRENT_DIR}/commands/publish.sh"

# shellcheck source=/dev/null
source "${CURRENT_DIR}/commands/scan.sh"

# shellcheck source=/dev/null
source "${CURRENT_DIR}/commands/up.sh"
